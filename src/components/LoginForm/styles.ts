import styled from "styled-components";

export const StyledLoginForm = styled.form`
  display: flex;
  flex-direction: column;
  gap: 10px;

  .FormError {
    font-size: 10px;
    color: red;
  }
`;
